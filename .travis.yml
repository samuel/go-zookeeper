language: go
go:
  - 1.4.2

branches:
  only:
    - master

before_install:
  - wget http://apache.claz.org/zookeeper/zookeeper-3.4.6/zookeeper-3.4.6.tar.gz
  - tar -zxvf zookeeper*tar.gz

install:
  - go get -t ./...

script:
  - go build ./...
  - go vet ./...
  - go test -v -race ./...
